import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miloolivera.gestorgastos',
  appName: 'HOME-Gestor de gastos',
  webDir: 'build'
};

export default config;
